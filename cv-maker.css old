:root{
  --bg:#070b1a;
  --card:#0d1330;
  --soft:#0b1028;
  --text:#eaf0ff;
  --muted:#b6c3ff;
  --brand:#4b7bff;
  --brand2:#30e7ff;
  --border:rgba(255,255,255,.08);
}

*{margin:0;padding:0;box-sizing:border-box;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial;}
body{background:var(--bg);color:var(--text);}

.topbar{
  position:sticky; top:0; z-index:999;
  display:flex; justify-content:space-between; align-items:center;
  padding:12px 16px;
  border-bottom:1px solid var(--border);
  background:rgba(7,11,26,.88);
  backdrop-filter: blur(12px);
  gap:12px;
  flex-wrap:wrap;
}

.brand{font-weight:900; display:flex; align-items:center; gap:10px; flex-wrap:wrap;}
.dot{
  width:12px; height:12px; border-radius:999px;
  background:linear-gradient(45deg, var(--brand), var(--brand2));
  box-shadow:0 0 24px rgba(48,231,255,.35);
}
.mini{font-size:12px; font-weight:700; color:rgba(234,240,255,.7);}

.actions{display:flex; gap:10px; flex-wrap:wrap;}

.btn{
  padding:10px 14px;
  border-radius:12px;
  border:1px solid var(--border);
  background:transparent;
  color:var(--text);
  font-weight:900;
  cursor:pointer;
}
.btn.primary{
  border:none;
  background:linear-gradient(45deg, var(--brand), var(--brand2));
  color:#081022;
}
.btn.ghost{background:rgba(255,255,255,.03)}
.btn:hover{transform:translateY(-1px)}

.select{
  border:1px solid var(--border);
  background:rgba(255,255,255,.03);
  color:var(--text);
  padding:10px 12px;
  border-radius:12px;
  font-weight:900;
  outline:none;
}

.layout{
  display:grid;
  grid-template-columns: 1.05fr .95fr;
  gap:12px;
  padding:12px;
  height: calc(100vh - 72px);
}

.panel{
  border:1px solid var(--border);
  border-radius:18px;
  overflow:hidden;
  background:rgba(255,255,255,.03);
  display:flex;
  flex-direction:column;
  min-height: 520px;
}

.panel-head{
  padding:14px;
  border-bottom:1px solid var(--border);
  background:rgba(255,255,255,.03);
}
.panel-head h2{font-size:16px}
.panel-head p{margin-top:6px;color:var(--muted);font-size:12px;line-height:1.5}

.panel-foot{
  padding:10px 14px;
  border-top:1px solid var(--border);
  color:rgba(234,240,255,.7);
  font-size:12px;
  background:rgba(7,11,26,.25);
}
.panel-foot code{
  padding:2px 6px; border-radius:8px;
  background:rgba(255,255,255,.08);
  border:1px solid var(--border);
}

.editor{
  flex:1;
  min-height: 420px;
}

.previewWrap{
  flex:1;
  background: rgba(255,255,255,.03);
  overflow:auto;
  padding:14px;
}

/* Resume page (white like PDF) */
.resume{
  background:white;
  color:#111;
  border-radius:14px;
  padding:22px 22px;
  min-height: 800px;
  width: min(820px, 100%);
  margin:0 auto;
  box-shadow: 0 20px 80px rgba(0,0,0,.45);
}

/* Resume typography */
.r-name{font-size:28px;font-weight:900;margin-bottom:6px}
.r-meta{font-size:12px;color:#333;margin-bottom:14px}
.r-sec{margin-top:14px}
.r-sec h3{
  font-size:13px;
  text-transform:uppercase;
  letter-spacing:.9px;
  margin-bottom:8px;
  border-bottom:1px solid #ddd;
  padding-bottom:6px;
}
.r-sec ul{padding-left:18px}
.r-sec li{margin-bottom:6px; line-height:1.35}
.r-para{margin-top:8px; line-height:1.6; color:#222}

@media(max-width:980px){
  .layout{grid-template-columns:1fr; height:auto;}
  .resume{min-height:600px}
}
